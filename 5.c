#include<stdio.h>
#include<stdlib.h>
int main(){
    int x;
    double y;
    float z;
    char ch;
    printf("The size of char datatype is %ld bytes\n",sizeof(ch));
    printf("The size of int datatype is %ld bytes\n",sizeof(x));
    printf("The size of float datatype is %ld bytes\n",sizeof(z));
    printf("The size of double datatype is %ld bytes\n",sizeof(y));
}